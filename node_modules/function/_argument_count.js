'use strict';

var _ = require('./PLACEHOLDER');

/**
 * Counts the number of non-placeholder arguments.
 * @private
 */
module.exports = function _argument_count(args /*:Array<any>*/) /*:number*/ {
	var count = 0;
	for (var i = 0; i < args.length; i++) {
		if (args[i] !== _) count++;
	}
	return count;
};
//# sourceMappingURL=_argument_count.js.map